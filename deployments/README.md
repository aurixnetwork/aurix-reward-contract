# Deployments

No contract has been deployed in this phase. `bsc-testnet.example.json` documents the report shape and intentionally contains null placeholders rather than a contract address or transaction hash.

Reviewed BSC Testnet deployment reports and verified explorer addresses should be added here after an explicitly authorized deployment. Never add private keys, RPC credentials, or API keys.
